# 原理图商业化工作流（源级管线 SOP）

> 2026-09-09 定稿。工具链在 `easyeda-harness/`，入口 `node wf.mjs`。
> 本流程由两天的实战教训固化而来——每条规则背后是一次真实事故。

## 铁律（P0-P5）

1. **文档源是唯一真理**（`getDocumentSource`/`setDocumentSource`）。对象 API 默认只做只读探针；尚未载入工程库的新器件按下述 `wf seed` 例外处理。
2. **已有电路修改走 `wf commit`**：存档回滚 → 纯函数变换 → 写前断言 → setDocumentSource → **save** → reload → 复读断言。不用对象 API 改接线或挪动已有器件。
3. **save() 是持久化门**：不 save 的写入 close 即弃、reopen 拉服务器版（"删除复活"一年悬案的真相）。
4. **文档身份钉死**：写入前后校验活动文档 uuid（用户切标签会漂移——守卫拦过一次写错 PCB）。
5. **确定性检查承担修复闭环**：`wf render` 只是源模型预览。每个修复批次从完整实时源、权威网表和真实 bbox 计算连接、碰撞、穿越、间距、对齐与图框门禁，并运行原生 DRC；这些项目不得交给截图判断。全部确定性检查收敛后，只截一次整页画布检查无法结构化表达的残余观感；重载后冻结的截图不算有效证据。
6. **棘轮**：新教训 → `assert_source.mjs` 新断言，只增不减。

## 日常操作

### AI 单次命中上下文契约

`wf check` 和 `wf audit` 每次都必须在仓库外生成 `workflow-context.json`。它是下一轮
修复的唯一交接入口，包含活动文档身份、捕获时间、源级状态、原生 DRC 计数、完整
token 覆盖、按优先级排序的修复队列，以及源文件、完整几何和原始报告路径。聚合型
检查必须同时给出可定位的器件、引脚、网络、线段、坐标或对象对；只报“若干处错误”
属于检查器缺陷，先完善检查器再改用户图纸。

程序负责文档身份、连通关系、正交、交叉/短接、穿越、bbox 碰撞、标签锚点与列、
模块/子区间距、图框禁区、安装脚接地、无源件邻接、标注完整和原生 DRC。AI 只判断
电路意图、器件选型，以及在程序已证明合法的候选中作选择。一个功能子区内的同类
问题必须一次成批修复；候选先用 `complete_geometry.json` 离线预检，写入后只做一次
保存、重载和复读。相同失败动作没有新增证据时不得重试。

### 新器件依赖与防止重复放置

新增未载入工程库的器件使用 `node wf.mjs seed <仓库外器件清单.json>`。
该入口仅调用官方 `sch_PrimitiveComponent.create` 添加器件：先核对文档、
系统库料号及供应商编号并备份整工程，逐个记录返回的真实图元 ID，保存后
检查原有源记录未变。随后的命名、位置、接线和 NC 状态仍通过 `wf commit` 完成。
`seed` 的成功仅表示器件创建并保存，不能当作布局或电气验收。

- 同一清单的回执存在时，入口拒绝再次创建；超时后先核对回执和真实图元，禁止盲目重试。
- 目标位号已经存在时拒绝创建，避免恢复会话后重复放置。
- 工程库中的器件、符号、封装 UUID 可能是新分配的本地 ID。验证其来源映射及实际内容，不要求它等于系统库 UUID。
- 导出、读取库信息或返回空结果的复制操作，不证明库依赖已经载入。没有新的调用条件或故障证据，不重复这条失败路径。
- 连通判断统一使用 `pointOnOrthogonalSegment` 的 `1e-6` 坐标容差；禁止用浮点数严格相等判断孤立导线。恢复旋转后的坐标噪声已有回归测试。
- 原生 `NO_CONNECT` 源记录使用实际导出结构；不要加入会被导入器丢弃的显示/字体属性。不能通过放宽网络名、引脚编号或 NC 值的复读比较消除失败。

### 以功能模块作为里程碑

先完成一个完整功能模块的选型、连接、可用区域与候选布局，再提交及综合核验。
同一模块内批量完成相关元件，不把每颗器件的放置或单项检查当作交付里程碑。
检查失败先定位到具体对象和坐标，修正候选后再写入。图元清理后必须比较模块外的
原生引脚连接，特别是删除所谓“孤立”导线时；局部几何通过不能证明外部网络未断开。
汇报完整功能块、实际检查结果及剩余条件，并保留最终整图验收要求。

原生 DRC 的聚合 API 在部分客户端不返回信息项，空数组也不能证明信息数为零。
缺失的信息数必须报告为未知，并从同一次检查的 DRC 界面或导出日志核对；
不要用日志侧栏累计条目数替代本次检查末尾的汇总。`sys_Log.sort()` 读取的是
系统日志，不保证包含独立的 DRC 日志。禁止通过清空日志或错误列表取得零计数。

```
node wf.mjs audit              # 源级断言 + 本次原生 DRC；通过不等于完整交付验收
node wf.mjs check              # 无截图：完整实时几何 token + 原生 DRC
node wf.mjs render [out.png]   # 源模型预览，另需真实画布核验
node wf.mjs commit t_xxx.mjs   # 提交一个纯函数变换
node restore_src.mjs archive_src_<时间戳>.txt   # 秒级回滚
```

运行前将 `EASYEDA_ARTIFACT_DIR` 指向仓库外的项目私有目录；可用
`EASYEDA_DOCUMENT_UUID` 固定目标页，用 `EASYEDA_WINDOW_ID` 固定已连接窗口。
入口不内置项目 ID，也不自动切换到其他项目。

`audit` 必须实际等待 `sch_Drc.check(..., true)` 完成，再解释详细返回。
任何 error、warning、info 或源级 finding 都不能通过；未知返回不能按零处理。
只有源级断言与 DRC 通过仍不足以交付：还需满足 `docs/schematic-design-rules.md`
的几何、连接、标签、图框/标题栏及符号语义规则。逐模块修复依赖完整坐标证据，
最终交付再按 `AGENTS.md` 提供一次新鲜整页视觉证据。
历史基线中的非零警告不是豁免条件。

仅当用户明确允许修复过程中保留其他待修项时，可为单次模块修复设置
`EASYEDA_REPAIR_STAGE=1`。变换必须声明 `stage.name/roots` 并执行
`validate(candidateSource)`：模块自身源级断言、接线、几何和边界须通过，
范围外电路语义须保持一致。保存后仍实际运行整页 DRC、核对原生网表并计算
完整实时几何 token；整页未清零就保持 `deliveryVerified:false`。这不是最终交付模式，
也不得作为默认忽略现有警告的开关。未获这种阶段修复授权时保持默认严格门禁。

## 变换编写规范

- `export default (recs) => ({ drop?, edit?, add?, note })`；事务会从**编译后的完整候选源**和基线真实几何自动构造 source twin、恢复候选网表并核对对象覆盖。私有 `validate(){return {pass:true}}` 不能替代完整裁判；新器件没有已验证几何 donor、既有引脚连通性变化或候选对象缺失时一律写前失败。
- drop 一个 COMPONENT/WIRE 会级联删其属性、线段和引脚覆盖；虚拟引脚父对象从实际符号读取，不猜 ID 前缀。
- 未知目标、重复 ID、悬空父对象和坏行在写入前拦截。写前所有源级 finding 均须清零，包括 soft 项。
- 保存重开后逐记录核对修改值及隐藏绑定；仅允许文档版本字段、记录排序和 ticket 变化。不能只验证删除成功。
- 大源文件经 bridge 发送时使用 `encodeBridgePayload` 的 ASCII JSON 转义；部分 bridge 版本逐 HTTP 分块解码，会在多字节字符边界引入乱码。发现复读乱码先比较具体记录并核对当前状态，不重交整个布局；不得把乱码加入允许差异。已有分块边界回归测试。
- 提交末尾实际运行 DRC；重载失败、保存值不同、DRC 非零或未知均不能宣称验证通过。
- 原始读取的 DOCHEAD 每次会更新 client/updateTime/version；并发修改守卫只排除这三个会话字段，仍校验文档身份和全部电路记录。
- 导入会归一化浮点坐标、空库属性、属性绘制顺序和派生引脚编号标注位置；复读比较只接受已验证的这些变化。网络名、显示状态、引脚编号、器件参数和非空封装绑定仍严格比较。
- **一组实际相连的导线必须属于同一个 WIRE**。不同 lineGroup 坐标接触不保证原生网表连通；生成时先用 `groupConnectedWires` 合并同网相接线段，再核对原生网表。
- **不生成隐藏 NET 属性**：当前导入会把部分 `valueVisible:false` 的 NET 改成可见，制造重复线名。内部网络靠同组导线连接，电源网靠真实旗标命名；只有声明了显示预算的信号才发 NET 标签。
- 属性按 `(parentId, key)` 更新；已有 `NO_CONNECT=no` 时必须改原记录，不能另加一条 `yes`。导入会合并重复属性，导致源级候选与实际不一致；事务编译器应在写前拒绝这种新增。
- 更换料号前先读取**原生解析后的封装 UUID**。源里的空 `Footprint` 可能从 `Device` 继承；更换 `Device` 后空值不再代表保留封装。需要显式绑定原 UUID，并复核原生器件、封装及制造网表。未加载的库绑定可能导致整个实例在导入时丢失，不能仅凭 DRC 数量判断器件仍然存在。
- 几何判断用**真盒模型**：`RIGHT_BOTTOM`=文字在锚点左下 `[x-w, x]×[y, y+8]`，字宽 4.6/字符（`getPrimitivesBBox` 实测标定）
- 区分原始源记录与 API/模型坐标：实测 `apiX = sourceX`、`apiY = -sourceY`。API 模型 y 向上；源级编辑必须转换一次，使用已有翻转的渲染器时不能再翻转。交付前按同一器件的源记录和实际引脚重新核对。
- **去重只限同线（同 parentId）**；跨线去重会吃掉设计意图（R=22 事故：吃掉 60 个逐脚标签）

## 断言清单（assert_source.mjs，事故溯源）

| 断言 | 事故来源 |
|---|---|
| A1 源完整性（坏行） | 解析健康 |
| A2 无 netport | 引擎 repair-nets 量产 105 个 netport |
| A3 标签真盒互叠=0 | 10 间距文字汤；估盒方向错导致 249 假叠 |
| A4 同线单标签 | SPI/XI 重试产生双标签 |
| A5 标签父线存活 | 复活线留下的孤儿标签 |
| A6 null 对齐标签(soft) | 引擎遗留 96 个，几何不可预测 |
| A7 旗标不重复 | 超时重试产生同位旗标堆叠 |
| A8 位号不重复 | — |
| A9 零长线段(soft) | 引脚吸附伪影 |
| A10 斜线=0 | 正交铁律（段式 i+=4 解析） |

## 布局语法基线（已验证形态）

- **10 间距信号列**：支线 25 + 同侧单列标签（文字贴线下面向本体，锚=自由端）
- **连接器双侧**：P/S 各自线下，绝不跨中线伸进邻座
- **电源密集列**：支线 + 轨名标签；轨源端（调节器/电源柱）用旗标
- GND 旗标每连接器一个做视觉锚（P12 链式）
- NC：未用脚全标（含连接器未焊脚）
